import {UnstyledButton} from "@mantine/core"
import cls from './red-btn.module.css'

export const RedBtn = () => {
  return (
    <UnstyledButton className={cls.btn}>Red Button</UnstyledButton>
  )
}
